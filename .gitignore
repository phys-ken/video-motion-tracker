# 第三者コード（CC BY-NC の IPhO アプリ / GPL の Tracker）— 概念参考用にローカル保管のみ。公開しない。
reference/

# 旧版の作業メモ・未使用アセットの退避先（履歴には残るので公開はしない）
archive/

# 診断用の一時スクショ（意図的な公開アセットではない）
image.png

# ユーザーが動作確認・分析用に持ち込んだ個人の撮影データ／書き出しデータ
# （IMG_XXXX.MOV はiPhone/iPad撮影のデフォルト命名。samples/配下の同梱サンプルは対象外）
IMG_*.MOV
IMG_*.mov
*.xlsx

# Node / テスト
node_modules/
package-lock.json

# Chrome ヘッドレス E2E の一時生成物
DevToolsActivePort

# OS / エディタ
.DS_Store
Thumbs.db

# アセット生成用のPython仮想環境（tools/gen_assets.py 用）
.venv/
__pycache__/
