# 動画解析トラッカー — 操作マニュアル

動画から物体の運動（位置・速度・加速度）を測定するツールです。
ブラウザ（Safari / Chrome）だけで動きます。**iPad は横向きが推奨**ですが、
スマートフォンの縦画面や PC でも使えます。

## 1. 全体の流れ

このアプリは**落体運動の4種**（自由落下・投げおろし／鉛直投げ上げ／水平投射／斜方投射）
に絞った専用ツールです。アプリを開くと、まず**どれを測るかを選ぶパネル**が出ます。

```
運動の種類を選ぶ → ① 動画を読み込む → ② スケール設定 → ③ 原点設定
               → ④ トラッキング → ⑤ データ出力
```

種類を選ぶと、**座標軸の正の向き**と**最初に出るグラフ**が自動で決まります。

| 選ぶもの | 正の向き | 最初に出るグラフ |
|---|---|---|
| 自由落下・投げおろし | y: **下**が正 | y–t, vy–t |
| 鉛直投げ上げ | y: **上**が正 | y–t, vy–t |
| 水平投射 | y: **下**が正 / x: 右が正 | y–x（軌道）, vx–t, vy–t |
| 斜方投射 | y: **上**が正 / x: 右が正 | y–x（軌道）, vx–t, vy–t |

選ぶまで動画は読み込めません（符号を決めずに測り始めないようにするため）。
選んだ種類は手順ガイドの左端に **`↑↓ 鉛直投げ上げ｜上向きが正`** のように常時表示され、
**タップすればいつでも選び直せます**。向きは表示上の変換なので、**あとから変えても
打った点はそのまま**使えます（グラフの符号だけが反転します）。

- ② スケールと ③ 原点は**任意**です（未設定なら px 単位で測れます）。
- 物理量（cm・cm/s・cm/s²）で出したいなら ②③ を先に済ませてください。

---

## 2. 画面の構成

横向きの基本レイアウトです。

```
┌─────────────────────────────────────────────────────────────────────┐
│ ▣ 動画解析トラッカー [v2026-…]       [動画を選択] [サンプルで試す]   │ ← ヘッダ
├─────────────────────────────────────────────────────────────────────┤
│ (↑↓鉛直投げ上げ|上向きが正) ①動画 ─ ②スケール ─ ③原点 ─ ④…    │ ← 手順ガイド
├──────────────────────────────────────────┬──────────────────────────┤
│ ┌ 動画スクリーン ──────────── [12 / 38] ┐ │ ▌コントロール            │
│ │                       ↑コマカウンタ常設│ │   FPS:[60]               │
│ │            ╋  ← 中央十字(照準)         │ │ ▌設定(キャリブレーション)│
│ │         (動画フレーム)                 │ │   [原点][スケール][スロー]│
│ │      ◀━━┫100 cm┣━━▶  ← スケール       │ │ ▌リアルタイムグラフ      │
│ └──────────────────── ズームリセット↑──┘ │   [おすすめ] ▢y-t ▢vy-t │
│ [▶] [|<] ────●──── [>|] 00:00/00:01      │ ▌測定データ [ストロボ][保存]│
│      範囲:全体  コマ:12/38  FPS:60        │   打点マップ [0][1][2]…  │
│                                            │ ▌複数の物体を追う・      │
│                                            │   自動色追跡 ▽（最下部） │
├──────────────────────────────────────────┴──────────────────────────┤
│        十字を対象に合わせて「確定」                                   │ ← 操作ヒント
│   [↶取消] [‹10] [‹1] [ ● 確定（点を打つ） ] [1›] [10›]              │ ← 下部固定バー
└─────────────────────────────────────────────────────────────────────┘
```

スマホ（縦画面）では、動画が上に固定され、下へスクロールすると
グラフ・打点マップ・数値表に届きます。下部の確定バーは常に表示されます。


**色の意味**（白ベースの画面）
- 🔵 青＝「操作・選択」（確定ボタン・中央の照準・現在ステップ・選択中の点）
- 🟠 アンバー＝「校正」（原点/スケール/スロー設定のボタンと値、映像上の原点軸・スケール線）
- 💗 マゼンタ＝「物体1の打点」（映像のマーカー・グラフの線・打点マップで共通。
  実写映像にほぼ出てこない色なので、どんな背景でも自分の打った点が見つかります）

---

## 3. ① 動画を読み込む

2通りあります。

- **[動画を選択]** … 端末内の動画ファイルを選ぶ（実験で撮った動画はこちら）。
- **[サンプルで試す]** … 同梱のサンプル動画で練習できる。

読み込みが終わると、**「読み込み完了」ダイアログ**が開き、
「43 コマ / 60 fps / 0.72 秒」のように動画の情報が表示されます
（動画ごとに実際のFPS＝1秒あたりのコマ数は違うため、アプリが自動で測定しています）。
サンプル動画でも同じように表示されます。

### そのまま「前後の余分」をカットしよう（おすすめ）

実験動画には、運動が始まる前・終わった後の「動きのないコマ」が入りがちです。
このダイアログの中でカットしてから始めると、あとの点打ちがラクになります。

1. ダイアログ内のスライダ（または [<] [>]）で**運動が始まるコマ**へ → **[ここから]**。
2. **運動が終わるコマ**へ → **[ここまで]**。
3. **[はじめる]** で閉じる（イン点のコマへ自動で頭出しされます）。

解析対象がその範囲だけになります（コマ送りも範囲内で止まります）。
あとから変えたいときは、再生バーの **[|<] [>|]** ボタンでいつでも設定・解除できます。
カットせず [はじめる] だけ押しても構いません。


---

## 4. ② スケール設定（長さの基準）

画面内の「**長さが分かっているもの**」（ものさし、既知の間隔など）を使って、
ピクセルと実寸（cm）の対応を教えます。

1. 右パネルの **[スケール設定]** を押す（ボタンが青く点灯）。
2. 映像を**1本指でドラッグ**して、中央の**十字を基準の片端**に合わせる。
3. **[確定]** を押す（始点）。
4. 十字を**もう一方の端**に合わせて、もう一度 **[確定]**（終点）。
   - 始点を確定した瞬間から、**始点と十字を結ぶ両矢印**が指について回り、
     いま何ピクセル分を選んでいるかが常に表示されます。
5. 「実際の長さ」を **cm** で入力 → OK。

設定後は、映像の上に **`100 cm` と書いた両矢印がそのまま残ります**。
「どこを何cmとして測ったのか」は測定全体の前提なので、あとから画面を見るだけで
確かめられるようにしてあります（右パネルにも `スケール: 0.500 cm/px` と出ます）。


### スロー動画の場合は「スロー設定」も

iPad/iPhoneの「スロー」モードで撮った動画は、書き出すと見かけ上のfps（例:30fps）と
実際の撮影fps（例:120fps・240fps）が食い違うことがあります。これに気づかず解析すると、
**速度・加速度が実際よりずっと小さく**計算されてしまいます。

1. 右パネルの **[スロー設定]** を押す。
2. カメラの「設定 > カメラ > スローモーション撮影」で選んだfpsを入力（120fps／240fpsのプリセット、または任意入力）。
   スローで撮っていない・わからない場合は「通常速度」のままでOK。

動画にスロー撮影の痕跡が見つかった場合は、読み込み時に自動で確認ダイアログが出ます
（見つからない場合もあるので、心当たりがあれば手動で設定してください）。
設定後、右パネルに `スロー: 240fps撮影 (8.00倍補正)` のように表示されます。

---

## 5. ③ 原点設定（座標の基準点）

測定の (0, 0) にしたい場所を決めます（例: 斜面の下端、発射点）。

1. **[原点を設定]** を押す（青く点灯）。
2. 映像をドラッグして**十字を原点にしたい位置**へ。
3. **[確定]**。

以後、x と y の**正の向きは、最初に選んだ運動の種類で決まります**
（例: 自由落下なら下が＋、投げ上げなら上が＋）。いま何が正かは手順ガイド左端の
チップと、グラフの縦軸ラベル（`y (cm) ↑正` など）でいつでも確認できます。


---

## 6. ④ トラッキング（位置を打つ）

ここが中心の作業です。**指で点を打つのではなく、中央の十字に対象を合わせて [確定] する**方式です
（指で隠れず、ぴったり正確に打てます）。

**操作**
| やりたいこと | 操作 |
|---|---|
| 映像を動かす（対象を十字へ） | **1本指でドラッグ** |
| 拡大・縮小 | **2本指でピンチ** |
| 拡大をリセット | スクリーン右下の ⛶ ボタン |
| 点を打つ | **[確定（点を打つ）]** |

**手順**
1. 追跡したい瞬間まで **[‹1] [1›]** でコマを送る（**長押しで連続送り**）。
   コマ送りは**新しいコマが横から押し込んでくるアニメーション**付きなので、
   進んだ・戻ったが必ず見えます。[‹10] [10›] は10コマ分をパラパラ漫画のように送ります。
2. 映像をドラッグ＆ピンチして、**十字を対象の中心**に合わせる。
3. **[確定]** を押す → その位置に点が記録され、**自動で次のコマへ進みます**。
4. また十字を合わせて [確定] … を繰り返すだけ。

> 映像の右上に常に「12 / 158」（現在のコマ / 総コマ数）が表示され、いま全体の
> どのあたりかが分かります。範囲の端まで来ると「最後のコマです」等のバッジが出ます。

**ふだん使わない設定は、右パネルの一番下に畳んであります**
（「**複数の物体を追う・自動色追跡**」）。ほとんどの授業では開く必要がありません。

- **ステップ幅** … 何コマおきに打つか。**既定の1のままで構いません。**
  v–t グラフの傾きから加速度を読む限り、精度はステップ幅にほとんど左右されません
  （実測で 1／2／3コマおきのいずれも誤差 0.7〜0.8%）。長い動画でタップ回数を
  減らしたいときだけ増やしてください。
- **追跡する物体 [物体1/2/3]** … 衝突など、2つ以上の物体を1本の動画で追うときだけ切り替えます。
- **オートトラック** … 色で自動追跡する補助機能。外すことがあるので打点マップで要確認。

**打点マップ**: 「測定データ」パネルに、解析範囲のコマ番号がチップで並びます。
**塗り＝打点済み・白抜き＝未打・太枠＝現在のコマ**。どのコマに座標が決まっているかが
一目で分かり、**チップをタップするとそのコマへ移動**できます（抜けの修正が最短）。
映像上に表示されるのは**現在のコマの点だけ**です（軌跡はストロボの「点マーカー」か
y–x グラフで見られます）。

**間違えたとき**
- 直前の点を取り消す … **[↶取消]**。「コマ12の点を取り消し」のように**何が消えたかが表示され、
  そのコマへ移動**します。打点マップの該当チップも点滅して場所を教えます。
- 過去の点を直す … **打点マップのそのコマをタップ**→十字を合わせ直して **[確定]** で上書き
  （「数値表を表示」を開けば、表の行タップでも移動できます）。
- 1点だけ消す … 数値表の行の **[削除]**、または点を選んで **[選択ポイントを削除]**。


> **自動保存**: 打ったデータは自動で保存され、ページを再読み込みしても残ります
> （動画だけは選び直してください）。

---

## 7. ⑤ グラフを見る

右パネルの「リアルタイムグラフ」は、点を打つたびに自動更新されます。
最初に選んだ運動の種類に合った組み合わせが既定で出ています。チェックボックスで
変更でき、**「おすすめ」ボタン**でも一括で切り替えられます。

**加速度（a–t）は既定では出しません。** 加速度は位置を2回微分するため、1点ごとの値は
クリック誤差が大きく増幅されて激しく上下します。**v–t グラフの傾きから読むのが正攻法**です
（次項）。どうしても点ごとの値を見たいときだけチェックを入れてください。

縦軸ラベルには `y (cm) ↑正` `vy (cm/s) ↓正` のように**正の向き**が付きます。
値が0をまたぐグラフでは **y=0 の横線を濃く**引くので、投げ上げで vy が正→負に
変わる瞬間（＝最高点）がひと目で分かります。

- **落下・投げ上げ** → y–t と vy–t
- **投射（放物線）** → y–x（軌道）と vx–t・vy–t

- **位置**: y–t（垂直）／x–t（水平）／y–x（軌道。**縦横同縮尺**で放物線の形が歪みません）
- **速度**: 速さ–t／vx–t／vy–t
- **加速度**: 加速度–t／ax–t／ay–t
  （速度・加速度は隣り合う点から自動計算＝数値微分）

### グラフを拡大して「傾き」を測る（重力加速度の測定）

グラフの**空いている場所をタップ**（または右上の ⤢）すると、そのグラフが**拡大表示**されます。

- グラフ上を**横にドラッグ**すると範囲を選べ、**その範囲の点だけの回帰直線の傾き**が
  上部に大きく表示されます（点線が回帰直線）。
- vy–t グラフなら **傾き＝平均加速度**。これが重力加速度の測定値になります。
  y–t なら傾き＝平均速度です。
- **点の数と R²（あてはまりの良さ。1に近いほど直線に乗っている）**も一緒に表示されます。
- タップ（ドラッグせず）または [選択解除] で全体の傾きに戻ります。

> **区間は「きれいなところだけ」ではなく、なるべく広く選んでください。**
> 実測とシミュレーションで確かめたところ、全区間を使えば g の誤差は **0.8%** ですが、
> 中央の10点だけに絞ると **7%** に悪化し、5%を外す確率が **47%** になります。
> 点が少ないほど、たまたまのクリック誤差に引きずられます。

**加速度グラフを出したときに両端が空いている理由**: グラフの端では、片側にしかデータがないため
加速度の誤差が中央の20倍以上に膨らみます（端の1〜2点だけが跳ね上がって見える現象の
正体です）。そこで**加速度は両端2点を計算対象から外し**、グラフにも出力にも出しません。
時間軸は他のグラフと揃えてあるので、両端が空いた形になります。速度は端でも十分な
精度が出るため全点表示します。

**生データ表示**: 既定では、クリック誤差やステップ幅の混在によるガタつきを抑えるため、
速度・加速度にスムージングがかかります（速度は前後2点、加速度はその速度に前後3点の
2次回帰を当てて傾きを読む）。「**生データ表示**」にチェックを入れると、スムージング
無しの厳密な数値微分に切り替わり、**端点も含めて全点**が表示されます
（真値既知のサンプル動画で精度を検証したいときなどに使ってください）。

**連動ハイライト**: グラフ・打点マップ・数値表・動画上の点は連動します。
グラフの点をタップすると、その点が**青で強調**され、動画もそのコマへジャンプします。


---

## 8. ⑥ データを出力

「測定データ」パネルの **[保存]** を押すと、出力ダイアログが開きます。
位置に加え、**速度・加速度の列も自動で付きます**。
（数値の一覧を画面で見たいときは、同じパネルの「数値表を表示」を開いてください。
普段は畳んであります。）

- **[TSVをコピー]** … クリップボードへ。Numbers / Google スプレッドシート / Excel に**そのまま貼り付け**できます。
- **[TSV保存]** … タブ区切りテキストとして保存。
- **[xlsx保存]** … Excel 形式で保存。

出力される列:

```
object_id, frame, t (s), x, y, vx, vy, v, ax, ay, a
```

（x など長さの単位は、スケール設定済みなら cm、未設定なら px。速度は /s、加速度は /s²。）

先頭には `#` で始まるメモ行が付き、スムージングON/OFFやスロー補正の有無など
「どの条件で出力したか」が分かるようになっています。


---

## 9. 操作早見表

| ジェスチャ / ボタン | はたらき |
|---|---|
| 1本指ドラッグ（スクリーン上） | 映像を動かす（パン） |
| 2本指ピンチ | 拡大・縮小 |
| [確定（点を打つ）] | 現在コマに点を記録 → 次コマへ |
| [‹1] / [1›] | 1コマ戻る／進む（**長押しで連続送り**） |
| [‹10] / [10›] | 10コマ戻る／進む（中間コマをパラパラ表示） |
| [↶取消] | 直前の操作を取り消し（消えた点のコマへ移動して表示） |
| [▶] | 再生／一時停止 |
| [\|<] / [>\|] | 現在のコマをイン点／アウト点に設定（もう一度押すと解除） |
| 打点マップのチップをタップ | そのコマへ移動（抜けたコマの打ち直しに） |
| グラフの点をタップ | その点を選び、そのコマへ移動 |
| グラフの空き地をタップ / ⤢ | グラフを拡大（横ドラッグで範囲の傾きを測定） |
| 数値表の行をタップ | その点を選び、そのコマへ移動（表は「数値表を表示」で開く） |

---

## 10. 困ったとき

- **動画が映らない / 真っ暗** … 端末によっては一部の動画形式が再生できないことがあります。
  標準のカメラ（MP4/H.264）で撮った動画を使ってください。`[サンプルで試す]` で動作確認を。
- **時刻や速度が変** … FPS が違う可能性。「コントロール」の **FPS** 欄に正しい値を手入力できます
  （手入力すると測定済みの時刻も計算し直します）。表示の `FPS: 30*` の `*` は「自動測定前／手動値」の印です。
- **重力加速度など、加速度が明らかに小さすぎる** … スロー動画の可能性があります。「4. スケール設定」の
  「スロー設定」を確認してください。
- **長い動画を読み込んでしまった** … クラッシュはしませんが、読み込み時のダイアログで
  **解析したい数秒だけに必ず絞ってください**（600コマを超えると警告が出ます。
  範囲が長いままだと打点マップが「打点済みのみ表示」に切り替わるなど、一部の表示が制限されます）。
- **コマ送りのアニメーションが出ない** … 端末の「視差効果を減らす／アニメーションを減らす」
  設定がONだと、意図的にアニメーションを省略します（機能は同じです）。
- **アプリの挙動がおかしい／説明と違う** … 画面下の「デバッグログ」を開き、
  **[レポートをコピー]** を押してください。バージョン・端末情報・現在の状態・操作ログが
  1つのテキストにまとまるので、そのまま貼り付けて報告できます。
  古いキャッシュのまま動いている場合は、ページを再読み込み（それでも直らなければ
  ブラウザのキャッシュ削除）してください。
- **コマがわずかにズレる** … 端末によってコマ単位のシークに誤差が出ることがあります。
  拡大して十字を合わせ直し、[確定] で上書きしてください。
- **色で自動追跡したい** … 右パネル**一番下**の「**複数の物体を追う・自動色追跡**」を開く → 一度手で点を打ってから
  「自動追跡」。背景が紛らわしいと外すので、外れたら手動で直してください（補助機能です）。
- **ストロボ写真** … 「測定データ」パネルの **[ストロボ]**。**写真（残像合成）**と、
  認識した点だけを丸で示す**点マーカー（散布図風）**の2つの表示を選べます。どちらもPNG保存できます。
- **データが消えた？** … 自動保存されています。同じブラウザで開き直し、動画を選び直せば続きから使えます。

---

## 11. 起動・配布

- **生徒**: 配布された URL をブラウザで開くだけ（インストール不要、動画はアップロードされません＝端末内で処理）。
- **先生**: 授業用の実験手順書サイト（`/guide/` 以下。自由落下・鉛直投げ上げ・水平投射・斜方投射）を
  併せて配布できます。アプリからはリンクしていないので、必要な回の URL だけ渡せます（印刷にも対応）。
- **先生 / 開発**: ローカルで動かす場合は同梱の `serve.py` を実行し、
  `http://localhost:8000`（同一LANの iPad からは `http://<PCのIP>:8000`）を開きます。
```
python serve.py
```

---

## サンプル動画の真値（教員向け）

「サンプルで試す」の各動画は `tools/gen_samples.py` で生成した合成動画で、
物理パラメータは以下の真値どおりです（g = 9.8 m/s²、60fps、H.264）。
画面内の「1 m」スケールバーで校正してから測ると、これらの値が再現できます。

| 動画 | 現象 | 向き | 真値 |
|---|---|---|---|
| 自由落下 | v0=0 の落下 | 縦 | 落下距離 約1.57 m（0.567 s） |
| 鉛直投げ上げ | 初速で上昇→落下 | 縦 | v0 = 4.4 m/s ↑、最高点 0.99 m、滞空 0.90 s |
| 水平投射 | 水平初速のみ | 横 | v0x = 5.0 m/s、v0y = 0 |
| 斜方投射 | 45°打ち上げ | 横 | v0 = 4.9 m/s（v0x = v0y = 3.46 m/s）、最高点 0.61 m、水平到達 2.45 m、滞空 0.71 s |
| 衝突（弾性） | 動く球→静止球 | 横 | m1:m2 = 2:1、u1 = 1.5 m/s → v1' = 0.5、v2' = 2.0 m/s |
| 衝突（合体） | 完全非弾性 | 横 | 等質量、u1 = 2.0 m/s → 合体後 v' = 1.0 m/s |

- スケール: 縦動画は 500 px = 1 m、横動画は 300 px = 1 m（バーで校正すれば意識不要）
- 衝突の動画には**アンバー色とシアン色の2つの球**が写っています。アプリの
  「物体1」「物体2」に割り当てて追うと、2物体トラッキングの練習になります
- 背景は教室風（黒板・窓・床・机）ですが、**すべてコードで描いた図形**です
  （写真素材は使っていないので、リポジトリごと自由に再配布できます）
- 再生成: `python tools/gen_samples.py`（numpy / OpenCV / ffmpeg が必要）
