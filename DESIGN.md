# 設計ノート — Video Motion Tracker

このアプリが「なぜこの形なのか」をまとめたものです。実装の詳細ではなく、
**設計上の判断とその理由**を残します（コードを読む前に目を通すと迷いません）。

対象は高校物理の授業で、生徒一人ひとりが自分の端末（iPad / スマートフォン）で
落体運動（自由落下・鉛直投げ上げ・水平投射・斜方投射）を測る場面です。

---

## 1. 何を最優先にしたか

1. **初めて触る生徒が迷わないこと。** 機能の多さより、次に何をすればよいかが
   分かることを優先します。手順ガイド（①動画 → ⑤出力）を常設し、いま行う段階を点灯させます。
2. **押したのに反応が分からない、をなくすこと。** 操作には必ず目に見える結果を返します
   （コマ送りのアニメーション、バッジ、コマカウンタ、打点マップ）。
3. **測った値が正しいこと。** 時刻 t の精度は、後段のすべて（速度・加速度・重力加速度）に
   効きます。ここは妥協していません（§3）。
4. **教室で確実に動くこと。** 外部CDNに一切依存せず、動画は端末内だけで処理します。

---

## 2. 中核インタラクション — 中央十字＋「確定」

**指で直接タップして点を打つ方式は採用していません。** タッチ操作では指が対象を隠し、
狙った位置に打てないためです。代わりに:

- 照準（十字）は**常に画面中央に固定**。
- 1本指ドラッグで映像を動かし、2本指ピンチで拡大して、**対象を十字に合わせる**。
- **[確定]** で現在のコマに点を記録し、ステップ幅ぶん自動でコマを送る。

モード切り替え（追跡モード／微調整モード等）は設けません。過去の点を直すときも
「そのコマへ移動 → 十字を合わせ直して確定で上書き」と作法が一貫します。
原点・スケールの設定も同じ「十字に合わせて確定」で行います。

**映像上に描くのは現在のコマの点だけ**です。軌跡を重ねると、修正時に「いま打った点が
どれか」が分からなくなるため、過去の点は打点マップ・y–x 軌道グラフ・ストロボ写真で見ます。

---

## 3. 時刻 t をどう正確にするか

動画の見かけのfps（例: 30fps）を信じて `t = frame / fps` とすると、可変フレームレート
（VFR）や複製フレームで時刻がずれ、加速度が狂います。次の三段構えで対処しています。

1. **コンテナ解析（既定）** … MP4Box.js で MP4/MOV のサンプル表を読み、
   各コマの実際の表示時刻（cts）を取得します。デコードもシークも不要で瞬時に終わり、
   VFRでもそのまま正確です。
2. **シーク走査（フォールバック）** … コンテナを解析できない形式のとき、
   実際にシークして各コマの時刻を測ります。短い動画に限定します。
3. **fps換算（最終手段）** … 上記が使えない端末では従来どおりの換算に劣化します。
   精度は落ちますが機能は壊れません。

さらに:

- **複製フレームの除外** … エンコード由来の同一コマは画素比較で検出して除きます。
- **スロー撮影の補正** … iPhone/iPad の「スロー」で撮った動画は、ファイルが名乗るfps
  （例:30）と実際の撮影fps（例:240）が食い違い、**加速度が数十分の一**に出ます。
  この差はファイルから復元できない場合があるため、自動検出（痕跡のヒント表示）に頼らず
  **手動入力を常に用意**しています。物理時間の倍率は
  `physicsFpsMultiplier` として保持し、**動画のシーク時刻には一切掛けません**
  （混同するとシーク自体が壊れます）。
- **解析範囲（イン点/アウト点）** … 運動していない前後を切り、その範囲だけを扱います。

---

## 4. シークの一貫性 — 「見えているコマ」に記録する

ブラウザの `currentTime` 設定だけでは、要求したコマが表示された保証がありません
（特に Safari は連続シークを間引きます）。そこで:

- シーク要求は**直列化キュー**で1件ずつ実行し、連打時は最後の要求だけを残します。
- 完了は **`requestVideoFrameCallback` と `seeked` の両方を張り、早い方**で確定します。
  video 要素は非表示のため、端末によっては前者が発火しません。片方に頼ると
  全シークがタイムアウト待ちになり「押しても動かない → たまに一気に飛ぶ」が起きます。
- 実測時刻による補正は**要求コマ±2以内の食い違いのみ**採用します。大きく離れた実測は
  遅れて届いた古い通知の疑いが強く、信じると大きく巻き戻ります。
- 点を打つときは、要求中のコマではなく **`displayedFrame()`（いま見えているコマ）** に
  記録します。見えているものと記録が必ず一致します。
- 確定の連打は**保留を1件だけ**にし、ユーザーが自分でコマを移動したら保留を破棄します
  （残った確定が後から発火して、戻る操作と喧嘩するのを防ぐため）。

---

## 5. 操作を目に見えるようにする

| 仕掛け | 解決している問題 |
|---|---|
| **プッシュ遷移**（新しいコマが送りの向きから入り、古いコマを押し出す） | 映像が暗い・変化が小さいと、コマが進んだか分からない。画面全体が動くので内容に依存せず伝わる |
| **パラパラ送り**（±10 は中間コマを1枚ずつ表示） | 10コマ飛ぶと何が起きたか分からない。1コマ最低60msの表示時間を保証し、遅い端末では時間予算を超えた時点で残りを直行 |
| **コマカウンタ**（映像右上に「12 / 158」を常設） | いま全体のどこにいるか分からない |
| **打点マップ**（コマ番号チップ。塗り＝打点済み・太枠＝現在。タップで移動） | どのコマに座標が決まったか、どこが抜けたかが分からない。戻って探す作業がいらなくなる |
| **取消の三重表示**（バッジ＋該当コマへ移動＋チップ点滅） | 取り消しで何が消えたのか分からない |

いずれも `prefers-reduced-motion` を尊重し、アニメーションを切った環境では静的に動きます。

---

## 6. データの出口

- **グラフ**は点を打つたびに更新。運動の種類に応じた**プリセット**
  （落下・投げ上げ／投射）で表示する量を一発で切り替えます。既定は y–t と vy–t です。
- **グラフを拡大 → 横ドラッグで範囲選択 → その範囲の回帰直線の傾き**を大きく表示します。
  v–t の傾きが平均加速度なので、**重力加速度の測定がアプリ内で完結**します。
  実践報告で繰り返し指摘される「位置グラフで満足して速度に進めない」壁への橋渡しです。
- **速度・加速度は既定でスムージング**（前後2点の2次回帰）。ステップ幅を混ぜても
  破綻しません。「生データ表示」で厳密な数値微分にも切り替えられ、出力にはどちらで
  計算したかをメモ行として記録します。
- **出力**は TSVコピー / TSV / xlsx と、**ストロボ写真PNG**（残像合成／点マーカーの2種）。

---

## 7. 見た目の方針

教室の昼間・プロジェクタ投影を前提とした**白ベース**です。原則は「色は意味にだけ使う」:

| 役割 | 色 | 対白コントラスト |
|---|---|---|
| 本文 | `#1F2933` | 14.8:1 (AAA) |
| 補助テキスト | `#52606D` | 6.5:1 |
| 操作・選択 | `#0B6BCB` | 5.3:1 |
| 校正（原点/スケール/スロー） | `#B45309` | 5.0:1 |
| 破壊操作 | `#B42318` | 6.6:1 |
| 物体1〜3（グラフ線・マーカー・打点マップで共通） | `#D81B8C` / `#D55E00` / `#009E73` | 4.7 / 3.9 / 3.4 |

- 物体1のマゼンタは、**実写映像にめったに現れない色相**であることを理由に選びました
  （空・肌・床・黒板と競合しません）。映像上のマーカーは白＋黒の二重縁取りで、
  背景の明暗によらず視認できます。物体色は色覚多様性でも分離するよう選定しています。
- 唯一の暗い面は**動画スクリーン**で、映像と照準に視線を集めます。
- 装飾は極薄の方眼のみ。影よりも罫線と余白で区切ります。
- 書体は端末内蔵のもの（ヒラギノ角ゴ等）。数値は等幅＋`tabular-nums` で桁を揃えます。

---

## 8. 動作環境と安全側の設計

- **サポート下限は iPadOS 15.4**（`requestVideoFrameCallback` と `svh` 単位の両対応ライン）。
  スマートフォンの縦画面にも対応し、動画を上部に固定して下へグラフ・打点マップ・数値表が続きます。
- **長い動画を誤って読み込んでもクラッシュしない**ようにしています。
  コンテナ解析は 96MB を超えるファイルでは全読みせず先頭16MB＋末尾48MBだけを読み、
  シーク走査は45秒超でスキップ、打点マップは範囲が400コマを超えるとチップを間引きます。
  読み込み時に「数秒だけに絞ってください」と案内します。
- **動画はアップロードされません。** すべて端末内（ブラウザ）で処理します。
- 計測データは自動保存し、**同じ動画を開いたときだけ**復元を提案します
  （名前・サイズ・長さの一致で判定）。無関係なデータが混ざるのを防ぐためです。
  保存キー名の `tracker_for_ipad_*` は旧名の名残ですが、既存の保存データを
  失わせないためそのままにしています。

---

## 9. 意図的に採用しなかったもの

- **PWA化** … iOS のストレージが一定期間で消える運用リスクと、Safari版とのデータ分離を嫌いました。
- **WebCodecs による厳密デコード** … 本用途には過剰で、HEVC の実装差も不安定です。
- **canvas の DPR 対応（高精細化）** … 現状は等倍描画です。Retina でわずかに甘くなりますが、
  メモリ上限（iOS Safari の canvas 総量）と引き換えになるため見送っています。
- **自動追跡の主役化** … 色による自動追跡は補助（折りたたみ）に留めています。
  背景が紛らわしいと静かに外すため、手動での修正が前提になるからです。

---

## 10. テスト

依存パッケージなしで動きます（Node 標準機能と、インストール済みの Chrome のみ）。

| コマンド | 内容 |
|---|---|
| `npm test` | 数値微分・スムージング・時刻表・スロー補正などのロジック単体テスト |
| `npm run test:e2e` | 実 Chrome を DevTools Protocol で駆動。動画を実際にデコードし、主要動線を検証 |
| `npm run test:precision` | 真値既知の動画から重力加速度を測り、**g = 9.8 ± 5%** と v–t の直線性を検証 |

`samples/` の動画は `tools/gen_samples.py` で生成した**真値が既知の合成動画**です。
教員が答え合わせに使えるよう、真値は MANUAL.md に一覧があります。

---

## 付録: 開発の経緯

2026年6月の初版から、iPad実機での不具合（読み込みの遅さ・コマずれ・レイアウト崩れ）の解消、
スロー動画対応、白ベースへのデザイン刷新、スマートフォン対応、と段階的に作り直しています。
各改修の詳細な検討過程は Git の履歴（コミットメッセージ）に残しています。
