# 設計ノート — Video Motion Tracker

このアプリが「なぜこの形なのか」をまとめたものです。実装の詳細ではなく、
**設計上の判断とその理由**を残します（コードを読む前に目を通すと迷いません）。

対象は高校物理の授業で、生徒一人ひとりが自分の端末（iPad / スマートフォン）で
落体運動（自由落下・鉛直投げ上げ・水平投射・斜方投射）を測る場面です。

---

## 1. 何を最優先にしたか

1. **初めて触る生徒が迷わないこと。** 機能の多さより、次に何をすればよいかが
   分かることを優先します。手順ガイド（①動画 → ⑤出力）を常設し、いま行う段階を点灯させます。
2. **押したのに反応が分からない、をなくすこと。** 操作には必ず目に見える結果を返します
   （コマ送りのアニメーション、バッジ、コマカウンタ、打点マップ）。
3. **測った値が正しいこと。** 時刻 t の精度は、後段のすべて（速度・加速度・重力加速度）に
   効きます。ここは妥協していません（§3）。
4. **教室で確実に動くこと。** 外部CDNに一切依存せず、動画は端末内だけで処理します。

---

## 2. 中核インタラクション — 中央十字＋「確定」

**指で直接タップして点を打つ方式は採用していません。** タッチ操作では指が対象を隠し、
狙った位置に打てないためです。代わりに:

- 照準（十字）は**常に画面中央に固定**。
- 1本指ドラッグで映像を動かし、2本指ピンチで拡大して、**対象を十字に合わせる**。
- **[確定]** で現在のコマに点を記録し、ステップ幅ぶん自動でコマを送る。

モード切り替え（追跡モード／微調整モード等）は設けません。過去の点を直すときも
「そのコマへ移動 → 十字を合わせ直して確定で上書き」と作法が一貫します。
原点・スケールの設定も同じ「十字に合わせて確定」で行います。

**映像上に描くのは現在のコマの点だけ**です。軌跡を重ねると、修正時に「いま打った点が
どれか」が分からなくなるため、過去の点は打点マップ・y–x 軌道グラフ・ストロボ写真で見ます。

---

## 3. 時刻 t をどう正確にするか

動画の見かけのfps（例: 30fps）を信じて `t = frame / fps` とすると、可変フレームレート
（VFR）や複製フレームで時刻がずれ、加速度が狂います。次の三段構えで対処しています。

1. **コンテナ解析（既定）** … MP4Box.js で MP4/MOV のサンプル表を読み、
   各コマの実際の表示時刻（cts）を取得します。デコードもシークも不要で瞬時に終わり、
   VFRでもそのまま正確です。
2. **シーク走査（フォールバック）** … コンテナを解析できない形式のとき、
   実際にシークして各コマの時刻を測ります。短い動画に限定します。
3. **fps換算（最終手段）** … 上記が使えない端末では従来どおりの換算に劣化します。
   精度は落ちますが機能は壊れません。

さらに:

- **複製フレームの除外** … エンコード由来の同一コマは画素比較で検出して除きます。
  ここは**感度を高く保つ**のが要点です。1画素あたりの差のしきい値を鈍くすると、
  **投げ上げの頂点や自由落下の出だし**のように「本当にほとんど動かないコマ」を
  複製と誤判定して捨ててしまいます（2026-08、頂点の2コマが消えて時間が飛ぶ不具合として発覚）。
  真の複製は画素が完全一致するので、感度を上げるほど誤爆せずに分離できます。
  実測（vertical_throw.mp4・54コマ）では、しきい値 d>24 だと頂点で変化画素が 0.049% まで
  落ちて判定ラインを割りましたが、d>6 なら最小でも 0.09% で割り込みません。
  全サンプルで「コンテナのコマ数＝アプリの採用コマ数」を自動テストで固定しています。
- **スロー撮影の補正** … iPhone/iPad の「スロー」で撮った動画は、ファイルが名乗るfps
  （例:30）と実際の撮影fps（例:240）が食い違い、**加速度が数十分の一**に出ます。
  この差はファイルから復元できない場合があるため、自動検出（痕跡のヒント表示）に頼らず
  **手動入力を常に用意**しています。物理時間の倍率は
  `physicsFpsMultiplier` として保持し、**動画のシーク時刻には一切掛けません**
  （混同するとシーク自体が壊れます）。
- **解析範囲（イン点/アウト点）** … 運動していない前後を切り、その範囲だけを扱います。

---

## 4. シークの一貫性 — 「見えているコマ」に記録する

ブラウザの `currentTime` 設定だけでは、要求したコマが表示された保証がありません
（特に Safari は連続シークを間引きます）。そこで:

- シーク要求は**直列化キュー**で1件ずつ実行し、連打時は最後の要求だけを残します。
- 完了は **`requestVideoFrameCallback` と `seeked` の両方を張り、早い方**で確定します。
  video 要素は非表示のため、端末によっては前者が発火しません。片方に頼ると
  全シークがタイムアウト待ちになり「押しても動かない → たまに一気に飛ぶ」が起きます。
- 実測時刻による補正は**要求コマ±2以内の食い違いのみ**採用します。大きく離れた実測は
  遅れて届いた古い通知の疑いが強く、信じると大きく巻き戻ります。
- 点を打つときは、要求中のコマではなく **`displayedFrame()`（いま見えているコマ）** に
  記録します。見えているものと記録が必ず一致します。
- 確定の連打は**保留を1件だけ**にし、ユーザーが自分でコマを移動したら保留を破棄します
  （残った確定が後から発火して、戻る操作と喧嘩するのを防ぐため）。

---

## 5. 操作を目に見えるようにする

| 仕掛け | 解決している問題 |
|---|---|
| **プッシュ遷移**（新しいコマが送りの向きから入り、古いコマを押し出す） | 映像が暗い・変化が小さいと、コマが進んだか分からない。画面全体が動くので内容に依存せず伝わる |
| **パラパラ送り**（±10 は中間コマを1枚ずつ表示） | 10コマ飛ぶと何が起きたか分からない。1コマ最低60msの表示時間を保証し、遅い端末では時間予算を超えた時点で残りを直行 |
| **コマカウンタ**（映像右上に「12 / 158」を常設） | いま全体のどこにいるか分からない |
| **打点マップ**（コマ番号チップ。塗り＝打点済み・太枠＝現在。タップで移動） | どのコマに座標が決まったか、どこが抜けたかが分からない。戻って探す作業がいらなくなる |
| **取消の三重表示**（バッジ＋該当コマへ移動＋チップ点滅） | 取り消しで何が消えたのか分からない |

いずれも `prefers-reduced-motion` を尊重し、アニメーションを切った環境では静的に動きます。

---

## 5.5 運動の種類を最初に選ばせる

対象を**落体運動の4種**（自由落下・投げおろし／鉛直投げ上げ／水平投射／斜方投射）に
絞ったので、起動時に「今日はどれを測るか」を1枚のパネルで選ばせ、**選ぶまで動画を
読み込ませません**。種類が決まれば **座標軸の正の向き** が決まるからです
（自由落下は下が正、投げ上げは上が正、投射は x が右向き正）。
生徒に符号の宣言をさせず、しかし符号を曖昧にしないための設計です。

- 符号は `physCoordOf()` での**表示上の変換**にとどめ、打点データ（動画ピクセル座標）は
  一切書き換えません。**あとから種類を変えてもデータは生き、グラフの符号だけが反転**します。
- 種類が決めるのは「軸の向き」と「最初に出すグラフ」の2つだけに絞りました。
  これ以上を自動化すると、生徒が「なぜそうなったか」を追えなくなります。
- 選んだ種類は手順ガイドの左端に常設し、タップで選び直せます。
  グラフの縦軸ラベルにも `y (cm) ↑正` のように向きを出し、常に確認できる状態を保ちます。
- 前回の打点が残っているときも、パネルは必ず出します。学校の共用 iPad では「前の人」の
  作業が残っていることがあり、そのモードを黙って引き継ぐと軸の符号が逆のまま測ってしまう
  からです。この場合は前回の種類を選択済みにもせず、脚注に「前回の打点（種類・点数）が
  残っています」と出します。途中で開き直した本人は、同じ動画を選び直せば「戻す」で
  打点もモードも復帰できるので、増えるのは1タップだけです。

## 5.5.1 「使う範囲を決める」画面

読み込み直後に出るこの画面は、次の方針で作っています。

- **見出しは「使う範囲を決める」**。「読み込み完了」だと、終わったことの報告にしかならず、
  次に何をすればいいかが伝わりません。
- **コマ番号は1始まりで表示**します。内部は0始まりですが、「80コマの動画」なのに
  「コマ 0 / 79」と出るのは同じ画面の中で数え方が食い違っていて分かりにくい
  （Nielsen の一貫性の原則）。表示は `frameNo()` を通します。
- **区間はスライダの帯で見せます**。使う側は青、切った側は灰色。文字だけだと、
  いま何が残るのかを読み取らないと分かりません。
- **［ここから］［ここまで］のボタン方式は変えません**。GOV.UK Design System は
  ドラッグ依存のレンジスライダーを避けるよう明記しており、ネイティブの
  `input[type=range]` はつまみを2つ持てません。自作の2つまみは、つまみ同士の重なりや
  タッチでの誤操作が起きやすい。プレイヘッド基準のボタン方式のほうが指に優しく、
  やり直しもききます。
- **押す部分は44px以上**（Apple HIG 44pt / Material 48dp）。
- **切らずに進めることを明記**します。任意のステップであること、あとから画面下の
  同じボタンでやり直せることを1行で添えています。

自由落下のサンプルには、この操作を授業で体験できるよう、運動の前後に静止区間を
入れてあります。静止していてもコマが「エンコーダの複製」と誤判定されないよう、
教室の掛け時計と窓の外の雲が動き続けます（判定は 160×90 に縮小して行うため、
ゆっくりした動きでは足りません。実測で余裕4倍以上を確認しています）。

## 5.6 加速度は「点ごとの微分」ではなく「v–t の傾き」で読ませる

生徒データ（60fps・全49点・鉛直投げ上げ）を解析したところ、点ごとの加速度は
**±942 cm/s²（真値 4878 の約20%）** も振れていました。60fps では1コマの変位が
1.4〜8 px しかなく、手打ちのクリック誤差（実測 σ=2.3 px）と同程度だからです。
2回微分の増幅率は 1/Δt² で効くため、これは原理的に避けられません。

一方、同じデータの **v–t 全区間の傾き**は g = 9.783 m/s²（誤差 **−0.17%**）でした。
モンテカルロ（σ=2.3px・500試行）でも 1σ 誤差は **0.8%**、しかも **ステップ幅を
1／2／3コマおきに変えても 0.7〜0.8% のまま**です。微分は局所の数点しか使わないのに対し、
回帰は全点を一度に使うためです。

この結果から:

- **a–t グラフは既定で表示しません**（求めるなら v–t の傾きから）。
- **ステップ幅は生徒に選ばせません**（精度に効かないので、選択のストレスだけが残る）。
  入力欄は最下部の折りたたみパネルへ移し、既定を1にしました。
- ただし**区間を狭く選ぶと悪化**します（中央10点で1σ 7%・5%超えが47%）。拡大画面に
  点数と R² を出し、広く選ぶ判断ができるようにしました。

## 5.7 原点は設定させない — 最初の打点が原点

原点は**映像の上に描きません**。自由落下の出だしのように変位が小さいと、原点の十字が
次の打点に重なって、狙った位置が見えなくなります。原点であること自体は生徒が意識する
必要がなく、グラフの軸ラベル（`y (cm) ↑正`）で足ります。


原点を生徒に設定させるのをやめ、**その物体の最初の打点を原点**にしました。
理由は2つあります。

- **操作が1つ減る。** 原点は「測り始めた場所」であることがほとんどで、
  わざわざ指定させる価値が薄い。
- **符号バグの根治。** 以前は「原点があるときだけ画面座標のyを反転」していたため、
  原点未設定のまま自由落下（下が正）を選ぶと**二重に反転して上が正**になっていました。
  原点が常に存在すれば、この分岐そのものが消えます。
  `physCoordOf()` は必ず `origin.y - p.y` で上向き正に揃えてから種類の符号を掛けます。

原点は保存せず毎回データから決めるので、最初の点を打ち直せば原点も追随します。
映像の上には原点の十字を常に描き、「どこが原点か」は目で確かめられるようにしています。

## 5.8 スケールは「モード」ではなく「ステップ」

スケール設定を、トラッキング画面の中の一時的なモードから、**通らないと先に進まない
ステップ**に変えました。実授業で「いまスケール設定中なのか点打ち中なのか分からない」
「間違えて点を打ってしまったときの戻り方が分からない」という報告が出たためです。

NN/g の指針は「**誤操作の代償が大きい場面ではモードを使わず操作系を分ける**」、
やむを得ない場合は「**冗長な手がかりを最低2つ**」です。そこで:

- 設定中は**帯・映像の枠の色・下部バーの構成**の3つが同時に変わります。
- そのあいだ **「点を打つ」ボタンは画面に存在しません**。取り違えが原理的に起きません。
- **[やり直す]** が常に隣にあるので、変な場所を確定しても戻れます。
- 習熟者向けの逃げ道として帯の右端に「スケールなしで進む」を置き、使うと
  手順ガイドに `⚠ スケール未設定（px）` が残り続けます（黙って px のまま進まない）。

## 5.9 提出物 — レポート画像と照合コード

提出のメインを**1枚のPNG**にしました。ストロボ写真とその運動の既定グラフを
A4縦（1:1.414）に組み、動画が縦長なら左右分割、横長なら上下分割に自動で切り替えます。

**照合コード**は「打った点だけ」（コマ番号と座標、および動画のファイル名・サイズ）の
SHA-256 を、読み間違えにくい31文字から選んだ8桁に畳んだものです。

- スケールを引き直しても変わらない（校正のやり直しで慌てさせない）。
- 座標を 0.1px 動かせば必ず変わる。
- 画像への**印字**・PNGの **tEXt チャンク**・**ファイル名**の3か所に入れます。
  スクリーンショットで提出されても、画面に見えている文字列で照合できます。
- これは**検出であって防止ではありません**。同じ画像が2人から出れば気づける、という設計です。

投げ上げのストロボは、上昇中と下降中を色で分けます。ただし**同じ高さを2回通る**ため、
塗りつぶし同士だと後から描いた方が完全に隠します。そこで**下降は中空の輪**にし、
重なった位置ではマゼンタの芯＋青い輪として両方が読めるようにしました。

### 5.9.2 出口は「実験結果を保存」の一本道

提出物はほぼ全員がストロボ画像なので、「ストロボ」と「保存」を並べず、
**［実験結果を保存］の一本道**にしました。押すと提出パネルが開き、そこで
見た目を決めて保存します。OKボタンは置きません（何が確定するのか伝わらないため）。
出口は「保存する」か「×で閉じる」だけです。

保存の届け方は端末で変えます。パソコンは直接ダウンロード、iPad・iPhone・Android は
共有シート（「写真に保存」が選べる）です。iPadOS は既定で Mac と同じUAを名乗るため、
UA だけでは MacBook と区別できません。**タッチ点数**（iPad=5 / MacBook=0）を併せて見ます。
判定は `isHandheldUA(ua, touchPoints)` という純粋関数にしてあり、実機がなくても
テストで確かめられます。

数表（xlsx / TSV）はめったに使わないので、画面いちばん下の地味なボタンに退避しました。

### 5.9.3 傾きは「注目した区間」で測る

v–t の傾きから g を読ませる設計ですが、全区間の傾きでは
**初めに止まっている区間**や、手を離す前後の乱れが混ざります。そこで提出パネルに
「グラフに傾きを表示する」チェック（既定オン）と「傾きの区間を決める」ボタンを置き、
**チェックが入っているのに区間が未設定なら保存させません**。

- 対象は **vy–t だけ**です。基準の違う数値（vx や 速さ の傾き）を同じ画像に並べません。
- 区間を決める画面は、グラフの全画面表示をそのまま使います（操作を覚え直さなくていい）。
  提出パネルとは別の overlay に載せているので、パネルは消えません。
- 選んだ区間はレポート画像にも**帯と回帰直線**として描かれ、チップに
  「選んだ区間 0.05–0.60 s の 17 点」と出ます。あとから見て何を測ったか分かるようにするためです。
- vy–t を表示しない設定のときは、傾きの話ごと隠します（保存も止めません）。

### 5.9.1 保存は「押す前に作っておく」

iOS Safari は、タップから時間の空いた保存（`await` をまたいで `<a download>` を押す等）を
黙って無視することがあります。提出画像の合成・ハッシュ計算・メタデータ埋め込みは重いので、
**設定が変わるたびに完成PNGまで先に作っておき**、[PNG保存] のタップでは同期的に届けるだけに
しました。届け方は3段構えです。

1. 共有シート（`navigator.share` にファイルを渡す）。「写真に保存」が選べるので行き先が分かりやすい。
2. 使えない端末は、従来どおり `<a download>` でファイルとして保存。
3. どちらも失敗したときのために、完成画像を `<img>` で画面に出しておく（長押しで「写真に追加」）。

完成PNGをそのままプレビューに出すので、生徒は**提出するものと同じ絵**を見て保存できます。

## 6. データの出口

- **グラフ**は点を打つたびに更新。運動の種類に応じた**プリセット**
  （落下・投げ上げ／投射）で表示する量を一発で切り替えます。既定は y–t と vy–t です。
- **グラフを拡大 → 横ドラッグで範囲選択 → その範囲の回帰直線の傾き**を大きく表示します。
  v–t の傾きが平均加速度なので、**重力加速度の測定がアプリ内で完結**します。
  実践報告で繰り返し指摘される「位置グラフで満足して速度に進めない」壁への橋渡しです。
- **速度・加速度は既定でスムージング**。速度は前後2点＝5点、加速度はその速度に
  前後3点＝7点の2次回帰を当てて傾きを読みます（「加速度＝v–tグラフの傾き」という
  意味を保つため、位置の2階微分としては求めていません）。ステップ幅を混ぜても
  破綻しません。「生データ表示」で厳密な数値微分にも切り替えられ、出力にはどちらで
  計算したかをメモ行として記録します。
- **端点の扱い**: 窓は端でも縮めず横にずらします。縮めると片側の少ない点で
  「窓の端の傾き」を求めることになり、クリック誤差の増幅が中央の8倍（加速度は
  それを2回かけるので23倍）に達して、端の1〜2点だけが跳ねるためです。
  ずらしても加速度は端ほど誤差が大きい（端から順に 296 / 221 / 147 / 80 cm/s²。
  クリック誤差1px・1px=0.5cm・30fps換算）ので、**加速度は両端2点を空欄**にして
  グラフ・出力から外します。速度は端でも誤差3%程度に収まるので全点残します。
  横軸だけは空欄の点も含めた全範囲で取り、縦に積んだグラフの時間軸を揃えています。
- **出力**は提出用PNG（ストロボ＋グラフ）が主で、TSVコピー / TSV / xlsx は画面最下部に退避。

---

## 7. 見た目の方針

教室の昼間・プロジェクタ投影を前提とした**白ベース**です。原則は「色は意味にだけ使う」:

| 役割 | 色 | 対白コントラスト |
|---|---|---|
| 本文 | `#1F2933` | 14.8:1 (AAA) |
| 補助テキスト | `#52606D` | 6.5:1 |
| 操作・選択 | `#0B6BCB` | 5.3:1 |
| 校正（原点/スケール/スロー） | `#B45309` | 5.0:1 |
| 破壊操作 | `#B42318` | 6.6:1 |
| 物体1〜3（グラフ線・マーカー・打点マップで共通） | `#D81B8C` / `#D55E00` / `#009E73` | 4.7 / 3.9 / 3.4 |

- 物体1のマゼンタは、**実写映像にめったに現れない色相**であることを理由に選びました
  （空・肌・床・黒板と競合しません）。映像上のマーカーは白＋黒の二重縁取りで、
  背景の明暗によらず視認できます。物体色は色覚多様性でも分離するよう選定しています。
- 唯一の暗い面は**動画スクリーン**で、映像と照準に視線を集めます。
- 装飾は極薄の方眼のみ。影よりも罫線と余白で区切ります。
- 書体は端末内蔵のもの（ヒラギノ角ゴ等）。数値は等幅＋`tabular-nums` で桁を揃えます。

---

## 8. 動作環境と安全側の設計

- **サポート下限は iPadOS 15.4**（`requestVideoFrameCallback` と `svh` 単位の両対応ライン）。
  スマートフォンの縦画面にも対応し、動画を上部に固定して下へグラフ・打点マップ・数値表が続きます。
- **長い動画を誤って読み込んでもクラッシュしない**ようにしています。
  コンテナ解析は 96MB を超えるファイルでは全読みせず先頭16MB＋末尾48MBだけを読み、
  シーク走査は45秒超でスキップ、打点マップは範囲が400コマを超えるとチップを間引きます。
  読み込み時に「数秒だけに絞ってください」と案内します。
- **動画はアップロードされません。** すべて端末内（ブラウザ）で処理します。
- 計測データは自動保存し、**同じ動画を開いたときだけ**復元を提案します
  （名前・サイズ・長さの一致で判定）。無関係なデータが混ざるのを防ぐためです。
  保存キー名の `tracker_for_ipad_*` は旧名の名残ですが、既存の保存データを
  失わせないためそのままにしています。

---

## 9. 意図的に採用しなかったもの

- **PWA化** … iOS のストレージが一定期間で消える運用リスクと、Safari版とのデータ分離を嫌いました。
- **WebCodecs による厳密デコード** … 本用途には過剰で、HEVC の実装差も不安定です。
- **canvas の DPR 対応（高精細化）** … 現状は等倍描画です。Retina でわずかに甘くなりますが、
  メモリ上限（iOS Safari の canvas 総量）と引き換えになるため見送っています。
- **自動追跡の主役化** … 色による自動追跡は補助（折りたたみ）に留めています。
  背景が紛らわしいと静かに外すため、手動での修正が前提になるからです。

---

## 10. テスト

依存パッケージなしで動きます（Node 標準機能と、インストール済みの Chrome のみ）。

| コマンド | 内容 |
|---|---|
| `npm test` | 数値微分・スムージング・時刻表・スロー補正などのロジック単体テスト |
| `npm run test:e2e` | 実 Chrome を DevTools Protocol で駆動。動画を実際にデコードし、主要動線を検証 |
| `npm run test:precision` | 真値既知の動画から重力加速度を測り、**g = 9.8 ± 5%** と v–t の直線性を検証 |
| `npm run test:views` | PC・タブレット横・タブレット縦・スマホの4画面で、起動から提出用レポートまでを通し、レイアウト崩れ（横スクロール・パネルの重なり・はみ出し）を検出 |

`samples/` の動画は `tools/gen_samples.py` で生成した**真値が既知の合成動画**です。
教員が答え合わせに使えるよう、真値は MANUAL.md に一覧があります。

---

## 付録: 開発の経緯

2026年6月の初版から、iPad実機での不具合（読み込みの遅さ・コマずれ・レイアウト崩れ）の解消、
スロー動画対応、白ベースへのデザイン刷新、スマートフォン対応、と段階的に作り直しています。
各改修の詳細な検討過程は Git の履歴（コミットメッセージ）に残しています。
